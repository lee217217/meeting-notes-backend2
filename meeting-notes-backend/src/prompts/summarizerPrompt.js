const summarizerSystemPrompt = {
  name: 'meeting_summarizer_phase1',
  content);
    }

    const payload = {
      meetingTitle: typeof body.meetingTitle === 'string' ? body.meeeee structured meeting summary.',
    'Focus on summary, key points, decisions, and open questions.',
    'Do not produce action items or email output.'
  ].join(' ')
};

module.exports = {
  summar);
    }

    const payload = {
      meetingTitle: typeof body.meetingTitle === 'string' ? body.meePrompt);
    }

    const payload = {
      meetingTitle: typeof body.meetingTitle === 'string' ? body.meeof body.meetingTitle === 'string' ? body.meeks from meeting content.',
    'Each task should include owner, due date, and priority when possible.',
    'Do not invent facts when information is missing.'
  ].join(' );
    }

    const payload = {
      meetingTitle: typeof body.meetingTitle === 'string' ? body.meempt.js`

```js
const followupEmailSystemProm);
    }

    const payload = {
      meetingTitle: typeof body.meetingTitle === 'string' ? body.mee typeof body.meetingTitle === 'string' ? body.meeional post-meeting follow-up email draft.',
    'Use the summary and action items as the source of truth.',
    'Do not send email or call external tools.'
  ].join(' );
    }

    const payload = {
      meetingTitle: typeof body.meetingTitle === 'string' ? body.mee'string' ? body.meeg' ? body.meemPrompt = {
  name: 'qa_review_phase1',
  content);
    }

    const payload = {
      meetingTitle: typeof body.meetingTitle === 'string' ? body.meeeeucture, and tone consistency.',
    'You may flag issues and lightly normalize output format.',
    'Do not invent missing facts.'
  ].join(' ')
};

module.exports = {
  qaReviewSystemPrompt
};
```

***

## 13) `src/services/llmClient.js`

這個檔案這一輪先放基礎版本，之後我們再接真實 AP);
    }

    const payload = {
      meetingTitle: typeof body.meetingTitle === 'string' ? body.mee: options && options.model ? opt);
    }

    const payload = {
      meetingTitle: typeof body.meetingTitle === 'string' ? body.mee  };
}

module.exports = {
  callLlm
};